#include <stdio.h>
#include <stdlib.h>
#include <strings.h>

// create a structure that holds question/answer pairs

// weighted graph adt implementation!

// create a function that reads the file and creates q/a pairs based on that (and adds to the graph!)

int main(){
  
  return 0;
}
